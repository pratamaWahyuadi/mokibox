# Secrets & local env
.env
.env.local
.env.*.local

# Build artifacts
bin/
*.exe
*.test
*.out

# Logs
*.log

# Local data volumes / runtime data
data/
pgdata/
redisdata/

# Sibling identity-provider stack (Zitadel) - lives
# outside the MokiBox source tree (see LLD asumsi A11).
# Default Makefile lookup is ./zitadel-compose/ but it
# can also be a sibling directory.
zitadel-compose/

# Go
vendor/

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
